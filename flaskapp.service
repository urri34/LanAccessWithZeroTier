[Unit]
Description=Aplicació Flask amb Gunicorn
After=network.target

[Service]
User=root
WorkingDirectory=/var/www
ExecStart=/var/www/venv/bin/gunicorn -b 127.0.0.1:8000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
